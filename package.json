{
  "name": "modular-form-creator",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter modular-form-creator-frontend dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "3.9.4"
  }
}
